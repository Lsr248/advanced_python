[build-system]
requires = ["setuptools >= 65", "wheel >= 0.38"]
build-backend = "setuptools.build_meta"

[project]
name = "tex_module"
version = "0.1"
description = "Python package for LaTeX table and images generation"

[tool.setuptools]
py-modules = ["tex_module"]